./result
result
result-*

# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml
artifacts

# Cargo (lib/rust is compiled by the flake; cargo is only for the LSP)
lib/rust/target
